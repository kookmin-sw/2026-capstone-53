# API 서버 URL (비워두면 http://localhost:8080/api/v1 사용)
REACT_APP_API_URL=http://localhost:8080/api/v1

# MSW Mock 서버 사용 여부 (true = MSW 사용, 그 외 = 실 서버 사용)
REACT_APP_USE_MOCK=true

# Web Push VAPID 공개키 (푸시 알림용, 없으면 구독 건너뜀)
# REACT_APP_VAPID_PUBLIC_KEY=
